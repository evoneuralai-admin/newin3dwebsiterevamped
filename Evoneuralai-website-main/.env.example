# LearnXR Environment Variables
# Copy to .env and fill in values. Never commit .env to git.
# Production: use Firebase Hosting env config or Secret Manager for Functions.

# Server (Express)
NODE_ENV=development
PORT=5002
SERVER_PORT=5002

# Firebase (for server/admin SDK)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
# Or path to service account JSON:
FIREBASE_SERVICE_ACCOUNT_PATH=

# OpenAI (AI features)
OPENAI_API_KEY=
OPENAI_AVATAR_API_KEY=

# Functions use Firebase Secret Manager; for local emulator, set in .env:
# BLOCKADE_API_KEY, MESHY_API_KEY, OPENAI_API_KEY, OPENAI_AVATAR_API_KEY
# LINKEDIN_ACCESS_TOKEN, LINKEDIN_COMPANY_URN

# Optional: Error monitoring (Sentry)
# VITE_SENTRY_DSN=
# SENTRY_DSN=

# --- learnxr.altiereality.com (hosted on altiereality site, backend = learnxr-evoneuralai) ---
# For altiereality build: do NOT set VITE_FIREBASE_PROJECT_ID=lexrn1.
# Auth, Firestore, Storage, and all Firebase Functions (proxy-asset, skybox/styles, etc.) must use learnxr-evoneuralai.
# Leave VITE_FIREBASE_* unset so defaults (learnxr-evoneuralai) apply, or set explicitly to learnxr-evoneuralai.
# If you set lexrn1 here, the app will talk to lexrn1 Firestore/Auth (no data/rules) and you will get "Missing or insufficient permissions".
# Optional: before building for altiereality, run from server/client: node scripts/check-altiereality-env.js (fails if VITE_FIREBASE_PROJECT_ID=lexrn1).
